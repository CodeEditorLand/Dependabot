name: Dependabot
on: [push]
jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/cache@v4.2.3
  ci:
    name: Ci
    runs-on: ubuntu-latest
    steps:
      - uses: actions/cache@v4.2.3
